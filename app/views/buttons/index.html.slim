p#notice= notice
h1 Buttons
ul
   - @buttons.each do |button|
      li.dashbutton = button.name
      - if button.avatar.attached?
         dashbutton = image_tag button.avatar.variant(resize: "100x100").processed
      br/
      = link_to 'Edit', edit_button_path(button)
      = link_to 'Destroy', button, method: :delete, data: { confirm: 'Are you sure??' }
      = link_to( '注文', items_path(button.items.build, item: {button_id: button.id}),  method: :post)  if button.order_allowed == true
br/
= link_to 'New Button', new_button_path
= link_to '買い物リスト', 'items'
= link_to 'Logout', logout_path, method: :delete
