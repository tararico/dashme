p#notice= notice
h1 買い物リスト
ul
  - @items.each do |item|
    li = item.button.name
    = item.description
    = link_to 'show', item
    = link_to 'コメント', edit_item_path(item)
    = link_to 'destroy', item, method: :delete, data: { confirm: 'Are you sure??' }
br/
= link_to 'Home', buttons_path
